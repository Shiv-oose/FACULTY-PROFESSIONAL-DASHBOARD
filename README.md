
  # Faculty Professional Track Dashboard

  This is a code bundle for Faculty Professional Track Dashboard.

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  
